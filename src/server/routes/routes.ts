const routes = {
  login: "/login",
  register: "/register",
  users: "/users",
  contact: "/contact",
  loadAllContacts: "/list",
  loadContact: "/:id",
  addNewContact: "/add-new-contact",
  deleteContact: "/delete/:id",
  updateContact: "/update/:id",
  loadContactsBySector: "/list/:sector",
};

export default routes;
